import React from "react";
import {Flex, Heading} from "@chakra-ui/react"


const UsernameNotSet: React.FC = () => {
	return (
		<Flex>
		<Heading>No username set so lets give you a name </Heading>
		</Flex>
	)
}

export default UsernameNotSet;
